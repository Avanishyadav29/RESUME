Resume Project - Beginner's Guide
A simple, semantic HTML resume project built using pure HTML5 semantic tags.
here is link of resume that is hosted :https://resumeavanish.netlify.app/
📸 Preview
Here's what the resume looks like:
<img width="869" height="824" alt="image" src="https://github.com/user-attachments/assets/8b6be152-d5df-42ab-92f7-11e07ec1acf7" />

# 📄 HTML Resume Project

This project is a simple **resume website built using semantic HTML5**.  
The main purpose of this project is to understand proper HTML structure, clean code practices, and basic accessibility without using inline styles.

---

## 📘 What I Learned

While working on this project, I learned the following:

### 1. Semantic HTML5 Tags
I understood the difference between semantic and non-semantic HTML elements and why semantic tags are important.

Tags used in this project:
- `<header>` – For name and job title
- `<main>` – Main container for resume content
- `<section>` – To divide content into sections
- `<article>` – For individual project details
- `<table>`, `<thead>`, `<tbody>` – For experience and education
- `<address>` – For contact information
- `<footer>` – For copyright text
- `<hr>` – To separate sections visually

### 2. HTML Structure
I learned how to organize content in a logical and readable way using proper nesting and headings.

### 3. Accessibility
Semantic tags help screen readers understand the page structure and improve accessibility.

### 4. Best Practices
- Clean and readable HTML
- No inline CSS
- Proper heading order (`h1`, `h2`, `h3`)

---

## 🛠️ Steps to Build the Resume

### Step 1: Basic Setup
- Created an `index.html` file
- Added HTML5 boilerplate using `<!DOCTYPE html>`
- Included meta tags for charset and responsive design

### Step 2: Semantic Layout
- Used `<header>` for top section
- Wrapped content inside `<main>`
- Created sections for:
  - About Me
  - Skills
  - Experience
  - Projects
  - Education
  - Contact Details

### Step 3: Content Addition
- Used lists for skills
- Tables for experience and education
- Articles for projects
- `<hr>` tags for section separation

### Step 4: Footer
- Added a footer with copyright information

## 💻 How to Run the Project Locally

### Method 1: Open in Browser
1. Go to the project folder
2. Double-click `index.html`
3. The resume will open in your default browser

OR  
- Drag and drop `index.html` into any browser window

### Method 2: Using Live Server (VS Code)
1. Open the project in VS Code
2. Install **Live Server** extension
3. Right-click on `index.html`
4. Click **Open with Live Server**

### 📤 Push Project to GitHub
Prerequisites

Git installed on your system

GitHub account

### Step 1: Initialize Git

Go to your project folder and run:
cd /Users/shivamgoyal/Desktop/Resume
git init

### Step 2: Create .gitignore (Optional)

Create a .gitignore file and add:
.DS_Store
*.log

### Step 3: Add Files

git add .

### Step 4: Commit Changes

git commit -m "HTML resume project"

### Step 5: Create GitHub Repository

Go to GitHub

Click New Repository

Enter repository name

Do not initialize with README

Create repository

### Step 6: Push Code to GitHub

git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

git branch -M main
git push -u origin main

### Step 7: Enable GitHub Pages

Go to repository Settings

Open Pages

Select branch main

Save

Your resume will be live at:
https://YOUR_USERNAME.github.io/YOUR_REPO_NAME/

### 📂 Project Structure

```
Resume/
│
├── index.html          # Main HTML file with resume content
└── README.md           # This file - project documentation
```

### 🤝 Contribution

This is a beginner-level learning project.
You can fork the repository, modify the content, and practice HTML concepts.

🎉 Happy Coding!

Keep learning and experimenting 🚀
